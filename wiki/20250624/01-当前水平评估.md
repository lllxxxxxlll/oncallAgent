# 当前学习水平评估：Eino 框架

> 评估时间：2026-06-24
> 基于：OncallAgent 项目实战 + test 目录脚手架代码分析
> ⚠️ **注意**：当前所有理解都来自**阅读代码**，尚未实际动手运行过 test 管道。
> 下面的"已掌握" = 看懂了，不等于亲手写过。

## 〇、待办

- [ ] **🔴 阶段一：让 test 代码跑起来**（详见 `03-下一步行动计划.md`）

---

## 一、已掌握的层次总览

```
Eino 了解程度：[████████░░] 55%（下调 5%，因为全是"看懂"不是"做过"）

   使用层  ████████░░  70%  能看懂、能改、没跑过
   原理层  ██████░░░░  50%  理解 Graph/链的运作方式，但细节不熟
   架构层  ████░░░░░░  30%  知道有哪些组件，没做过设计决策
```

## 二、各层详细盘点

### 使用层 — 已掌握（全为代码阅读）

- ✅ 理解 Eino 的核心抽象：`Component → Graph → Runnable → Runner`
- ✅ 认识 5 种节点类型：Loader、Transformer、Indexer、Lambda、ChatModel、Retriever
- ✅ 理解 Graph 构建流程：`NewGraph → AddXXXNode → AddEdge → Compile → Invoke/Stream`
- ✅ 知道两种触发模式的存在：`AllPredecessor` vs `AnyPredecessor`
- ✅ 能看懂项目的 Chat Pipeline 架构图（RAG + ReAct 并行）
- ✅ 能看懂项目的 Knowledge Index Pipeline 架构图（串联管道）
- ✅ 会用 Callback 观察节点的输入输出（`log_call_back`）

### 使用层 — 还不会

- ❌ 独立从零搭建一个 Eino Pipeline（需要抄模板）
- ❌ 实际跑通并调试一个 Eino Pipeline（尚未动手）
- ❌ 添加自定义 Lambda 节点（不知道怎么写签名）
- ❌ 用 `Stream` 模式跑管道（只会 `Invoke`）
- ❌ 理解 InputKey/OutputKey 的匹配规则

### 原理层 — 已掌握

- ✅ 知道 Graph 维护一个内部状态字典（`map[string]any`）
- ✅ 知道 `WithOutputKey` 把节点输出写到状态字典中
- ✅ 知道 `WithInputKey` 从状态字典读取输入
- ✅ 理解 Callback 的 AOP 机制（`OnStart` / `OnEnd` 在节点执行前后插入）
- ✅ 理解 Go 接口 + Eino 组件的关系：`interface` 在 eino 核心，`实现` 在 eino-ext

### 原理层 — 还不清楚

- ❌ `Compile()` 内部做了什么（拓扑排序？DAG 合法性校验？）
- ❌ `AllPredecessor` 的等待机制怎么实现（channel？semaphore？）
- ❌ Tool Calling 是怎么在 Eino 中实现的（Tool→ChatModel 的数据流）
- ❌ `Stream` 模式下 Graph 状态怎么传递
- ❌ 节点间的类型匹配是怎么校验的（编译期还是运行时？）

### 架构层 — 已了解

- ✅ 知道 Eino 分为四层：components / compose / flow / adk
- ✅ 知道 Eino 的竞争力在于 Go 语言 + 类型安全 + 字节开源
- ✅ 项目中有三种 Agent 模式：ReAct、Plan-Execute-Replan、RAG Pipeline

### 架构层 — 还不会

- ❌ 不能做技术选型（Eino vs LangChain vs 自研，说不出 trade-off）
- ❌ 不知道什么时候用 Graph 什么时候用 Chain
- ❌ 不清楚 Eino 的并发模型和性能瓶颈
- ❌ 没读过 eino compose 核心源码

---

## 三、当前最核心的 3 个知识缺口

1. **InputKey/OutputKey 的匹配机制** — 这是 Graph 数据流的核心，当前只知道"写了就能读"，不知道重名冲突、类型不匹配时怎么处理。

2. **Compile 到 Invoke 的内部流程** — `BuildGraph → Compile → Runnable → Invoke` 这一条链的内部实现。

3. **Tool Calling 的完整链路** — LLM 如何决定调用哪个工具？参数怎么传？结果怎么返回给 LLM？
